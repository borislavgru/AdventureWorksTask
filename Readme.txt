1. Starting the backend server:
- Open the AdventureWorksTask.sln from Backend folder
- Run the solution

2. Starting the frontend (prerequisite is to have node.js installed):
- open command line in frontend directory
- run command npm install
- run command npm start
